// Confirms the jest-expo preset loads without config errors (T0b). Safe to
// delete once real tests land (T3/T4 in docs/tickets/worker-active-job-flow.md).
test("jest-expo preset is wired up", () => {
  expect(true).toBe(true);
});
